<%= form_for([@book, @book.reviews.build]) do |f| %>
  <div class="field" id="rating-form">
    <%= f.label :rating %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment, class: "form-control" %>
  </div>

  <div class="field mt-2">
    <%= f.submit yield(:button_text), class: "btn-custom2" %>
  </div>
<% end %>

<script>
  $('#rating-form').raty({
    path: '/assets/',
    scoreName: 'review[rating]'
  });
</script>